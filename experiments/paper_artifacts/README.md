# JCP Experiment Artifacts

Generated by:

```bash
python3 experiments/generate_jcp_artifacts.py
```

## Main-text eligible

- `table_schur_variants.csv`: use `production_eligible=true` rows for performance claims.
- `table_adaptation_ablation.csv`: `1/2/3` adaptation-step ablation on `8x8`, three seeds.
- `table_external_baselines.csv`: external velocity-block solver comparison.
- `figure_setup_vs_size.png`: pure and Schur-optimized setup scaling.
- `figure_wall_time_vs_size.png`: pure and Schur-optimized wall-time scaling.

## Validation-only

- `table_unstructured_3d_poisson.csv`: 3D unstructured linear-tetra manufactured Poisson validation.
- `table_unstructured_3d_stokes.csv`: small 3D unstructured blocked Stokes/Picard validation with residual and fallback diagnostics.
- The `64x64` Schur-optimized point is a scalability smoke result. Meta has zero velocity/full fallback, but the traditional hierarchy has six velocity direct fallbacks, so it is marked `production_eligible=false` and must not be used as a fair speedup claim.

## Required wording

- Separate `pure_iterative` and `schur_optimized_lu` results.
- Schur-LU uses one cached sparse velocity-block LU per Picard step inside pressure Schur applications; final velocity solves still use AMG-preconditioned Krylov.
- The 3D Stokes path is operational for small linear-tetra blocked solves, but no mesh-refinement convergence or large 3D performance claim has yet been established.
